# config.yaml
hydra:
  run:
    dir: .

dynamics_model_checkpoint: '/store/real/zhanyis/diffusion_policy/outputs/2025-04-17/16-03-00/checkpoints/model_150.pth'
policy_checkpoint: '/store/real/zhanyis/diffusion_policy/data/outputs/2025.04.16/22.57.50_train_diffusion_unet_hybrid_pushT_image/checkpoints/580.ckpt'
guidance_start_timestep: 10
guidance_scale: 0.05

threshold: 3.7
n_test: 50
test_start_seed: 100000
num_samples: 8
n_action_steps: 8

output_dir: 'data/release_pushT_test'

device: "cuda"

# Environment runner configuration
env_runner_target: 'diffusion_policy.env_runner.pusht_image_sequential_runner.SequentialPushTImageRunner'

# Planner configuration
planner_target: 'dyn_model.planner_pusht.PushTPlanner'

# Demo dataset path
demo_dataset_path: 'path_to_expert_dataset'

